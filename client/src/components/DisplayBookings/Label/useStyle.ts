import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  heading: {
    fontWeight: 'bolder',
    fontSize: '0.8rem',
  },
  name: {
    fontWeight: 'bolder',
    fontSize: '1rem',
  },
  date: {
    fontWeight: 'bolder',
    fontSize: '1rem',
  },
  status: {},
}));

export default useStyles;
