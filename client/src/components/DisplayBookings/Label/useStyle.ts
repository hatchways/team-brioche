import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  heading: {
    fontWeight: 'bolder',
    fontSize: '12px',
  },
  name: {
    paddingLeft: '1rem',
    fontWeight: 'bolder',
    fontSize: '12px',
  },
  date: {
    fontWeight: 'bolder',
    fontSize: '12px',
  },
  status: {},
}));

export default useStyles;
